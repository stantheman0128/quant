# 期末海報 — 丟給 Claude（claude.ai）的交接 prompt

> 用途：把 A0 學術海報的排版工作整包交給 claude.ai（沙箱有 LibreOffice，能自己渲染＋視覺 QA）。
> 用法：到 claude.ai 開新對話，**上傳下面 3 個檔案**，再把「==== PROMPT ====」以下整段貼進去。

## 要上傳的檔案
1. `docs/期末海報_內容_2026-06-15.md` —— 文字內容唯一真相來源（必上傳）
2. `海報範例.pptx` —— 要模仿的版型／樣式（必上傳）
3. `KOL_Poster_A0_v2.pptx` —— 現有草稿，當起點（建議上傳）

---

==== PROMPT ====

你是一位學術海報設計專家。請幫我做一張 **A0 直式（84.1 × 118.9 cm）單頁學術研究海報**，輸出成 `.pptx`。

## 輸入檔案（我已上傳）
- `期末海報_內容_2026-06-15.md` —— **海報所有文字的唯一來源**，照它寫，不要自己改寫或新增數據。
- `海報範例.pptx` —— **請模仿這張海報的版型與視覺語言**（系所制式範例）：頂部 logo＋中英標題＋作者／指導教授一行、標題下一條分隔線、段落用「深色長條 bar 上放白色粗體標題」、英數用 serif、內文左右對齊。
- `KOL_Poster_A0_v2.pptx` —— 我之前產的草稿，**版面結構可沿用當起點**，但它有明確缺陷要修（見下）。

## 必修的已知問題（重點）
草稿 v2 的文字框都開太大、內文填不滿，導致**下半部大片空白、上下不平衡**：
- Motivation 內文結束後到 Methodology bar 之間空約 16cm
- Methodology 內文到 JSON schema 框之間空約 26cm
- Our Ideas 內文到 Reference 之間空約 35cm

→ **請重新平衡版面，讓內容均勻填滿整張 A0**：可加大內文字級、放大段距與行距、放大流程圖與表格、或在欄內重新分配，使兩欄高度相當、沒有突兀的大留白，也不可有重疊／溢出。

## 版面結構（A0 直式）
```
┌────────────────────────────────────────────┐
│ [logo]  標題(中) / 標題(英)            [No.50] │  Header
│         作者 · 指導教授 · 系所                  │
├────────────────────────────────────────────┤
│  全寬「System Overview 系統流程」彩色方塊流程圖 │  6 box + 箭頭 + 圖例
├──────────────────────┬─────────────────────┤
│ ■ Motivation 研究動機 │ ■ Experimental Results│
│ ■ Methodology 方法    │   （表1 + 表2 + 註記） │  雙欄
│   （4 步 + JSON schema│ ■ Our Ideas 重點與定位 │
│     程式碼框）         │                       │
├──────────────────────┴─────────────────────┤
│ Reference 參考文獻（底部全寬）                  │
└────────────────────────────────────────────┘
```

## 設計 token（從同學海報抓出，務必照做）
- **段落標題**：白色粗體文字放在**深紅色長條 bar** 上（不是紅字加底線）。紅 = `#92232A`。
- **流程圖 6 方塊配色**：深藍 `#1A1A2D`、藍 `#1F78B4`、綠 `#1A9541`、紫 `#8A2E9F`、紅 `#92232A`（最後一格 Incremental IC 用紅，強調核心）。
- **字型**：英文／數字 **Times New Roman**（serif）；中文 **微軟正黑體**；程式碼／schema 用 **Consolas**。
- **內文色**：主文 `#202833`、深色強調 `#1A1A2D`、次要說明 `#5C6B7A`。
- **表格**：表頭深色底白字；最佳／Ours 那列（表1 的「C 高信心過濾」、表2 的「>0.60」）用**淺紅底 highlight**。
- 背景白、內文黑、段落左右對齊。**標題以外的內文不要置中。**
- ⚠ 絕對不要在標題下加裝飾性底線（那是 AI 味的記號）；用留白或 bar 本身區隔。

## 內容重點（完整文字以 md 為準，這裡列關鍵不可漏的）
- **標題(中)**：中文財經 KOL 觀點作為選股因子：對 Alpha101 的增量資訊檢定
- **標題(英)**：Chinese Financial KOL Opinions as a Stock-Selection Factor: Testing Incremental Information over Alpha101
- **作者**：Po-Han Shih（施博瀚）｜**指導教授**：Prof. Po-Wen Chi（紀博文）｜NTNU, Dept. of CSIE｜編號 No. 50
- **系統流程 6 格**：KOL 貼文(FB) → LLM-as-Formatter(格式化·不評分) → 4 欄位 Schema{t,id,dir,weight} → 每日 KOL 因子 f_kol → 移除 Alpha101(橫斷面正交化) → Incremental IC(殘餘預測力)
- **JSON schema 程式碼框**：
  ```json
  { "timestamp": "2026-03-15T09:00:00+08:00",
    "id": "2330", "direction": +1,
    "weight": 0.65 }   // walk-forward 命中率
  ```
- **表 1. 三種訊號策略比較**（表頭：策略 / hit / IC / Sharpe）：
  - A 純方向 — 54.3% / +0.045 / +1.11
  - B walk-forward 加權 — 55.0% / **+0.097** / +0.97
  - **C 高信心過濾 — 57.9% / — / +1.65 ★**（highlight）
- **表 2. Walk-forward 權重分位的命中率**（表頭：weight 分位 / n / hit rate）：
  - <0.45 — 276 — 49.3%
  - 0.55–0.60 — 271 — 55.0%
  - **>0.60 — 180 — 62.2% ★**（highlight）
- **誠實註記（務必保留）**：⚠ 上表為 raw IC，尚未扣 Alpha101；incremental IC（核心驗收）計算進行中。
- **PoC 規模**：3 位 FB KOL · 1,685 觀點 · 404 標的 · 2024–2026。
- **Reference**：[1] Kakushadze (2016) 101 Formulaic Alphas；[2] Pardo (2008) Walk-Forward Analysis；[3] Bailey & López de Prado (2014) Deflated Sharpe Ratio；[4] Kakhbod et al. (2023) Finfluencers。

## 評分配重（用來決定各段深度）
研究問題描述・價值・挑戰 **20%**｜方法／系統可行性與創新 **40%**｜實驗分析完整性與有效性 **40%**。
→ 兩個 40%（方法、實驗）給最多版面與細節；20%（Motivation）點到為止。

## 交付與品質要求（QA）
1. 輸出單頁 `.pptx`，頁面尺寸正好 84.1 × 118.9 cm。
2. **做完後，把投影片轉成圖片，用子代理（subagent）以全新視角做視覺 QA**，逐項檢查：重疊、文字溢出／被切、欄位對齊、上下留白是否平衡、低對比、字級對比是否足夠、leftover 佔位文字。
3. 至少跑一輪「修正→重新渲染→再檢查」，確認沒有大片突兀留白、沒有重疊後才算完成。
4. 全程繁體中文（技術術語可保留英文）；數據一字不改照上面與 md。

==== PROMPT 結束 ====
